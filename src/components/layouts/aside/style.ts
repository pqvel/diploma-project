import stylex from '@stylexjs/stylex';

export const style = stylex.create({
    aside: {
        display: "flex",
        flexDirection: "column",
        maxWidth: 320,
        width: "100%",
        height: "100%",
    }
})
  